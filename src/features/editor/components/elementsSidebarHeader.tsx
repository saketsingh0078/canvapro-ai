import React from "react";

const ElementsSidebarHeader = ({ label }: { label: string }) => {
  return <div className="px-2 py-1 font-bold">{label}</div>;
};

export default ElementsSidebarHeader;
